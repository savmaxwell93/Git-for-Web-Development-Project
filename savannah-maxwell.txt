https://codepen.io/savmaxwell93/pen/vYZvMbW?editors=1100
Module 2 Questions

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? 
	HTML that introduces meaning to the page
    2. What is HTML used for? 
	Html is the backbone of a webpage, it sets up the structure and information (ie. text, images, etc.) that will be found on the page
    3. What is an attribute and where do we put it? 
	A modifier of an html element used inside the opening tag that controls the element’s behavior
    4. What is the h1 tag used for? How many times should I use it on a page? 
	Generally used for the title of the page. Once.
    5. Name two tags that have required attributes.
	img (<img src=“”>) and a (<a href=“”>)
    6. What do we put in the head of our HTML document? 
	Data about the html document
    7. What is an id? 
	An attribute that specifies a unique value of an element and is used to point to a specific style declaration in CSS
    8. What elements can I add an id to? 
	Any element
    9. How many times can I use the same id on a page? 
	Once
    10. What is a class? 
	An attribute that specifies a value on an element, and can be used to point to a class name in CSS
    11. What elements can I add a class to? 
	Any element
    12. How many times can I use the same class on a page? 
	Infinite
    13. How do I get my link to open in a new tab?
	_blank
    14. What is the alt attribute in the image tag used for? 
	It provides alternative text for an image if the image cannot be displayed
    15. How do I reference an id?
	#id
    16. What is the difference between a section and a div?
	Section means that the content inside is grouped together, a div does not convey any meaning aside from any attributes attached
    17. What is CSS used for? 
	CSS is used to style a page. (ie. Give it color, fonts, sizes etc.)
    18. How to we select an element? Example - every h2 on the page
	h2
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
	An id is a unique value for an element, is pointed at by using #, and each id can only be used once. An id could be used for your header, footer, or a menu that appears once
	A class is a value that can be used on elements that appears multiple times throughout a page. A class could be used for paragraphs in multiple sections to style them all the same
    20. How do we select classes in CSS?
	.classname
    21. How do we select a p element with a single class of “human””?
	.human p
    22. What is a parent child selector? When would this be useful? 
	A tool used to select elements with a specific parent. Could be used when you have multiple paragraphs within a single div element that you want to be styled the same as each other but different than other paragraphs on the page
    23. How do you select all links within a div with the class of sidebar?
	.sidebar div
    24. What is a pseudo selector?
	Used to define a special state of an element, such as when a user mouses over it
    25. What do we use the change the spacing between lines?
	line-height
    26. What do we use to change the spacing between letters?
	letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
	text-transform:uppercase; text-transform:lowercase; text-transform:capitalize;
    28. How do I add a 1px border around my div that is dotted and black?
	div {
		border: 1px dotted black;
	}
    29. How do I select everything on the page? 
	body {}
    30. How do I write a comment in CSS?
	/*comment*/
    31. How do I find out what file I am in, when I am using the command line? 
	pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
	ls -la
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
	rm filename -if you’re not careful you can permanently delete files you didn’t mean to remove
    34. Why should I use version control? 
	It works as a backup system so you can still access older versions
    35. How often should I commit to github?
	You should commit every change made
    36. What is the command we would use to push our repo up to github? 
	git push -u origin first name-lastname
    37. Walk me through Lambda's git flow. 
	Fork the repo to create your own copy
	Clone your forked repo via SSH
	Via the terminal navigate to where the files should go and type “git clone url”
	Change your directory into the new repository via “cd reponame”
	Create a new branch “git checkout -b firstname-lastname”
	Use “code .” To open the project in VS code
	Complete the project
	Push the changes back to git hub:
		-“git add .” To add all the changes to the staging area
		-“git commit -m ‘message’” to commit to the changes you made with a detailed message as to what those changes are
		-“git push -u origin firstname-lastname” to create a remote version of your branch and push the changes to git hub
	Create a pull request, but don’t merge it, to let everyone know that you have made change, by pushing “Compare and pull request”
	Choose your repository as the base and compare to the master then press “Create pull request”
	Submit the url to Canvas
	

Stretch Questions

    1. What is the difference between an inline element and a block element?
	Inline elements do not force a new line to begin in the document, while block elements typically cause a line break
    2. What happens when an element is positioned absolutely? 
	The element is positioned relative to the nearest positioned ancestor, if no ancestor is found it uses the document body and moves w/ page scrolling
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
	Use the flex-grow property from Flexbox
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
	Display Block: <address> <header> <nav>
	Display Inline: <span> <strong>
	Inline-Block: <button>
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 






































